using System;
using TaetArray;
using static System.Net.Mime.MediaTypeNames;
internal static class ArrayHelpers
{

    private static int Vowel(string text)
    {
        int size[5] = { 0 };
        for (int i = 0; i < txt.Length; i++)
        {
            if (txt[i] == 'A' || txt[i] == 'a')
            {
                size[i] = 1;
            }
        }
    }
        private static int Vowel(string text)
        {
            int size[5] = {0};
            for (int i = 0; i < txt.Length; i++)
            {
                if (txt[i] == 'A' || txt[i] == 'a')
                {
                    size[i] = 1;
                }
            }
        }
}